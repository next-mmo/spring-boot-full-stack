# Compounding Solutions

Solutions record verified fixes and reusable technical patterns so future work can avoid repeating the same failure.

Use `NNNN-kebab-case-title.md` with YAML frontmatter. The `solve` command creates numbered drafts; complete and verify them before relying on native recall.
